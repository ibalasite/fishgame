# Machine-Readable Contracts Index

Generated by `gendoc-gen-contracts` from gendoc documentation pipeline.  
Source documents: docs/EDD.md, docs/API.md, docs/SCHEMA.md, docs/ARCH.md

## Artifact Catalog

| Artifact | Path | Purpose | Source |
|----------|------|---------|--------|
| OpenAPI 3.1 YAML | [docs/blueprint/contracts/openapi.yaml](blueprint/contracts/openapi.yaml) | API 合約，可匯入 Postman / Swagger UI / code generator | docs/API.md + EDD.md §10.6 |
| JSON Schema — RegisterRequest | [docs/blueprint/contracts/schemas/RegisterRequest.schema.json](blueprint/contracts/schemas/RegisterRequest.schema.json) | 帳號註冊請求欄位驗證 | API.md §2.1 |
| JSON Schema — LoginRequest | [docs/blueprint/contracts/schemas/LoginRequest.schema.json](blueprint/contracts/schemas/LoginRequest.schema.json) | 登入請求欄位驗證 | API.md §1.1 |
| JSON Schema — AuthTokenPairDTO | [docs/blueprint/contracts/schemas/AuthTokenPairDTO.schema.json](blueprint/contracts/schemas/AuthTokenPairDTO.schema.json) | JWT Token 對格式定義 | EDD.md §10.6 |
| JSON Schema — UserDTO | [docs/blueprint/contracts/schemas/UserDTO.schema.json](blueprint/contracts/schemas/UserDTO.schema.json) | 玩家資料 DTO（PII 遮罩） | EDD.md §10.6 |
| JSON Schema — BalanceDTO | [docs/blueprint/contracts/schemas/BalanceDTO.schema.json](blueprint/contracts/schemas/BalanceDTO.schema.json) | 玩家金幣/鑽石餘額 DTO | EDD.md §10.6 |
| JSON Schema — OrderDTO | [docs/blueprint/contracts/schemas/OrderDTO.schema.json](blueprint/contracts/schemas/OrderDTO.schema.json) | IAP 訂單 DTO | EDD.md §10.6 |
| JSON Schema — VipSubscriptionDTO | [docs/blueprint/contracts/schemas/VipSubscriptionDTO.schema.json](blueprint/contracts/schemas/VipSubscriptionDTO.schema.json) | VIP 訂閱狀態 DTO | EDD.md §10.6 |
| JSON Schema — PurchaseRequest | [docs/blueprint/contracts/schemas/PurchaseRequest.schema.json](blueprint/contracts/schemas/PurchaseRequest.schema.json) | IAP 充值請求驗證 | API.md §2.3 |
| JSON Schema — VipSubscribeRequest | [docs/blueprint/contracts/schemas/VipSubscribeRequest.schema.json](blueprint/contracts/schemas/VipSubscribeRequest.schema.json) | VIP 訂閱請求驗證 | API.md §2.1 |
| JSON Schema — ProductDTO | [docs/blueprint/contracts/schemas/ProductDTO.schema.json](blueprint/contracts/schemas/ProductDTO.schema.json) | 商城商品 DTO | SCHEMA.md §3.10 |
| Pact — CocosClient→AccountService | [docs/blueprint/contracts/pact/CocosClient--AccountService.json](blueprint/contracts/pact/CocosClient--AccountService.json) | 帳號/認證 consumer-driven 合約（5 interactions） | API.md §1, §2.1 |
| Pact — CocosClient→ShopService | [docs/blueprint/contracts/pact/CocosClient--ShopService.json](blueprint/contracts/pact/CocosClient--ShopService.json) | 商城/VIP consumer-driven 合約（5 interactions） | API.md §2.3 |
| Pact — GameService→CommerceBC | [docs/blueprint/contracts/pact/GameService--CommerceBC.json](blueprint/contracts/pact/GameService--CommerceBC.json) | 遊戲結算/Jackpot 服務間合約（2 interactions） | EDD.md §10.8 |
| Docker Compose | [docker-compose.yml](../docker-compose.yml) | 本地/Staging 部署配置（api+game+admin+mysql+redis+unleash） | EDD.md §10.3 |
| Seed Code — Domain Layer | [docs/blueprint/scaffold/src/domain/](blueprint/scaffold/src/domain/) | 骨架代碼（空實作）— Fish, BossFish, JackpotService, User, Order | EDD.md §10.6 Class Diagram |
| Seed Code — Application Layer | [docs/blueprint/scaffold/src/application/](blueprint/scaffold/src/application/) | Application Service interfaces | EDD.md §10.6 |
| Seed Code — Infrastructure | [docs/blueprint/scaffold/src/infrastructure/](blueprint/scaffold/src/infrastructure/) | Repository interfaces | EDD.md §3.1 |
| Seed Tests | [docs/blueprint/scaffold/tests/](blueprint/scaffold/tests/) | 失敗佔位測試（TDD 起點） | EDD.md §10.6 + 不變量表 |

---

## Usage Guide

### OpenAPI / Swagger UI

```bash
# Validate
npx @redocly/openapi-cli lint docs/blueprint/contracts/openapi.yaml

# Generate client SDK (TypeScript — Cocos Creator 用)
npx openapi-generator-cli generate \
  -i docs/blueprint/contracts/openapi.yaml \
  -g typescript-axios \
  -o src/generated/client

# Start Swagger UI
npx swagger-ui-serve docs/blueprint/contracts/openapi.yaml
```

### JSON Schema Validation

```bash
# Validate a payload against RegisterRequest schema
npx ajv validate \
  -s docs/blueprint/contracts/schemas/RegisterRequest.schema.json \
  -d payload.json

# Validate all schemas
for f in docs/blueprint/contracts/schemas/*.schema.json; do
  npx ajv compile -s "$f" && echo "✅ $f"
done
```

### Pact Contract Testing

```bash
# Provider verification (AccountService)
npx pact-verifier \
  --pact-urls docs/blueprint/contracts/pact/CocosClient--AccountService.json \
  --provider-base-url http://localhost:3000 \
  --provider-states-setup-url http://localhost:3000/_pact/setup

# Provider verification (ShopService)
npx pact-verifier \
  --pact-urls docs/blueprint/contracts/pact/CocosClient--ShopService.json \
  --provider-base-url http://localhost:3000 \
  --provider-states-setup-url http://localhost:3000/_pact/setup
```

### Deploy with Docker Compose

```bash
# Start all services
docker-compose up -d

# Run Prisma migrations
docker-compose exec api npx prisma migrate deploy

# Seed initial game_configs
docker-compose exec api npx prisma db seed

# View logs
docker-compose logs -f game
```

### Scaffold

```bash
# Copy seed code to src/ to begin implementation
cp -r docs/blueprint/scaffold/src/ src/

# Copy test skeletons
cp -r docs/blueprint/scaffold/tests/ tests/

# Run failing placeholder tests (TDD starting point)
npx jest --testPathPattern="scaffold"
```

---

## Coverage Report

| Step | Output | Count | Status |
|------|--------|-------|--------|
| OpenAPI 3.1 | openapi.yaml | 16 paths, 40+ schemas | ✅ |
| JSON Schema | schemas/ | 10 DTO schemas | ✅ |
| Pact Contracts | pact/ | 3 pairs, 12 interactions total | ✅ |
| IaC | docker-compose.yml | docker-compose（6 services） | ✅ |
| Seed Code | scaffold/src/ | 9 files（TypeScript） | ✅ |
| Seed Tests | scaffold/tests/ | 3 test files（failing placeholders） | ✅ |

### Notes

- OpenAPI 未涵蓋：WebSocket（Colyseus）事件（shootFish, FishKilledEvent 等）— 這些透過 Colyseus Schema State Sync 定義，非 REST
- Pact missing_payloads：GameService→CommerceBC 為 Redis Pub/Sub 異步事件，Pact 合約描述邏輯語義，非 HTTP 端點
- deploy_target=docker；若未來改為 k8s，重新執行 gendoc-gen-contracts 生成 helm/values.yaml + network-policy.yaml

---

*Generated: 2026-04-25*  
*gendoc version: D17-CONTRACTS*
